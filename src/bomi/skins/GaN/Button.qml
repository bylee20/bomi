import QtQuick 2.0
import bomi 1.0 as B

B.Button {
    size: 32
    property string iconName: ""
//    icon.source: getStateIconName(iconName, hovered || (bind && bind.hovered), pressed || (bind && bind.pressed))
}
