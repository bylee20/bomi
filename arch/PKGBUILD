# Maintainer: xylosper <darklin20@gmail.com>

pkgname=('cmplayer')
pkgver=0.7.0
pkgrel=1
arch=('i686' 'x86_64')
url="http://code.google.com/p/$pkgname"
license=('GPL')
depends=('qt>=5.0' 'libdvdread' 'libcdio' 'openal' 'alsa-lib' 'mpg123' 'libva' 'libgl' 'yasm')
makedepends=('git' 'mesa' 'gcc>=4.7')

build() {
	cd $srcdir
	echo $url
	echo $pkgname-$pkgver
	git clone $url $pkgname-$pkgver
	cd $pkgname-$pkgver
	git checkout $pkgver
	./build-libchardet && ./build-ffmpeg && ./build-mpv && \
	make PREFIX=/usr CXX=g++ QMAKE=qmake-qt5 LRELEASE=lrelease-qt5 cmplayer || return 1
}

package() {
	pkgdesc="CMPlayer is a multimedia player."
	cd "$srcdir/$pkgname-$pkgver"
	make DEST_DIR=$pkgdir PREFIX=/usr CXX=g++ QMAKE=qmake-qt5 LRELEASE=lrelease-qt5 install || return 1
}
